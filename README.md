# Machine Learning on Tic-tac-toe game
Tic-tac-toe game with machine learning abilities.

To compile just run `make`.

To run just run `./main`.

File `net.mlx` will be created - this is database of neurons and synapses.

To run automatic learning with random you can enter `a` letter, or `m` to play with self taught algorithm.
